apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sonarr-config
  labels:
    app: tofu-controller
    env: production
    category: core
resources:
  - sonarr-tf-credentials-sealedsecret.yaml
  - terraform-cr.yaml
