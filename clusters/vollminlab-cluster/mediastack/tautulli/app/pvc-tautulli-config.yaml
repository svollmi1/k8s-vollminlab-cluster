apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tautulli-config
  namespace: mediastack
  labels:
    app: tautulli
    env: production
    category: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: smb
  resources:
    requests:
      storage: 1Gi
