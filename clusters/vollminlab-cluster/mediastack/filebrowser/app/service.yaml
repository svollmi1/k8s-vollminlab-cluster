apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: mediastack
  labels:
    app: filebrowser
    env: production
    category: apps
spec:
  selector:
    app: filebrowser
  ports:
    - name: http
      port: 80
      targetPort: 80
