apiVersion: v1
kind: Service
metadata:
  name: exportarr
  namespace: mediastack
  labels:
    app: exportarr
    env: production
    category: media
spec:
  type: ClusterIP
  selector:
    app: exportarr
  ports:
    - name: metrics
      port: 9707
      targetPort: metrics
      protocol: TCP
