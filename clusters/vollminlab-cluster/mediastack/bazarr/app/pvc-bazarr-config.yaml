apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-bazarr-config
  namespace: mediastack
  labels:
    app: bazarr
    env: production
    category: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
